name: fitapp
description: A FitApp mobile application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  firebase_core: ^2.18.0

  # Dipendenze base (versione compatibile con Flutter 3.16.0)
  characters: ^1.3.0
  collection: ^1.18.0
  material_color_utilities: ^0.11.1
  meta: ^1.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0  # Versione compatibile

flutter:
  uses-material-design: true
  assets:
    - assets/images/